auto-install-peers=false
strict-peer-dependencies=false

# Allow installs in CI (e.g. Cloudflare Pages) when lockfile/config
# (overrides, catalogs, etc.) would otherwise fail a frozen install.
# Local non-CI behavior is unchanged (frozen is already off by default).
frozen-lockfile=false
